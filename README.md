# weather-ml-project
interactive web platform that allows users to compare multiple machine learning models for predicting next-day temperature at a select location using historical data

# clone repo
git clone https://github.com/Strongic/weather-ml-project

# creating virtual environment and installing requirements
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt